import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.codingtechnyks.gps',
  appName: 'proyectoverano',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
